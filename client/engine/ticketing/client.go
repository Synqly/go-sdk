// Code generated by Fern. DO NOT EDIT.

package ticketing

import (
    context "context"

    engine "github.com/synqly/go-sdk/client/engine"
    core "github.com/synqly/go-sdk/client/engine/core"
    internal "github.com/synqly/go-sdk/client/engine/internal"
    option "github.com/synqly/go-sdk/client/engine/option"
)


type Client struct {
    WithRawResponse *RawClient

    options *core.RequestOptions
    baseURL string
    caller *internal.Caller
}

func NewClient(options *core.RequestOptions) *Client {
    return &Client{
        WithRawResponse: NewRawClient(options),
        options: options,
        baseURL: options.BaseURL,
        caller: internal.NewCaller(
            &internal.CallerParams{
                Client: options.HTTPClient,
                MaxAttempts: options.MaxAttempts,
                DisableRetries: options.DisableRetries,
            },
        ),
    }
}

// List all remote fields for all Projects in a ticketing integration. The response will include a list of fields for each issue type in the ticketing provider.
func (c *Client) ListRemoteFields(
    ctx context.Context,
    request *engine.ListRemoteFieldsRequest,
    opts ...option.RequestOption,
) (*engine.ListRemoteFieldsResponse, error){
    response, err := c.WithRawResponse.ListRemoteFields(
        ctx,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Returns a list of `Projects` from the token-linked `Integration`. Tickets must be created and retrieved within the context of a specific Project.
func (c *Client) ListProjects(
    ctx context.Context,
    request *engine.ListProjectsRequest,
    opts ...option.RequestOption,
) (*engine.ListProjectsResponse, error){
    response, err := c.WithRawResponse.ListProjects(
        ctx,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Returns a list of `Ticket` objects from the token-linked `Integration`.
func (c *Client) QueryTickets(
    ctx context.Context,
    request *engine.QueryTicketsRequest,
    opts ...option.RequestOption,
) (*engine.QueryTicketsResponse, error){
    response, err := c.WithRawResponse.QueryTickets(
        ctx,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Returns a `Ticket` object matching `{ticketId}` from the token-linked `Integration`.
func (c *Client) GetTicket(
    ctx context.Context,
    ticketId engine.TicketId,
    request *engine.GetTicketRequest,
    opts ...option.RequestOption,
) (*engine.GetTicketResponse, error){
    response, err := c.WithRawResponse.GetTicket(
        ctx,
        ticketId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Creates a `Ticket` object in the token-linked Integration.
func (c *Client) CreateTicket(
    ctx context.Context,
    request *engine.CreateTicketRequestInput,
    opts ...option.RequestOption,
) (*engine.CreateTicketResponse, error){
    response, err := c.WithRawResponse.CreateTicket(
        ctx,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Updates the `Ticket` object matching `{ticketId}` in the token-linked `Integration`.
func (c *Client) PatchTicket(
    ctx context.Context,
    ticketId engine.TicketId,
    request *engine.PatchTicketRequestInput,
    opts ...option.RequestOption,
) (*engine.PatchTicketResponse, error){
    response, err := c.WithRawResponse.PatchTicket(
        ctx,
        ticketId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// [beta: currently supported by Jira] Creates an `Attachment` for the ticket with id `{ticketId}` in the token-linked `Integration`.
func (c *Client) CreateAttachment(
    ctx context.Context,
    ticketId engine.TicketId,
    request *engine.CreateAttachmentRequestInput,
    opts ...option.RequestOption,
) (*engine.CreateAttachmentResponse, error){
    response, err := c.WithRawResponse.CreateAttachment(
        ctx,
        ticketId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// [beta: currently supported by Jira] Returns metadata for all Attachments for a `Ticket` object matching `{ticketId}` from the token-linked `Integration`.
func (c *Client) ListAttachmentsMetadata(
    ctx context.Context,
    ticketId engine.TicketId,
    request *engine.ListAttachmentsMetadataRequest,
    opts ...option.RequestOption,
) (*engine.ListAttachmentsMetadataResponse, error){
    response, err := c.WithRawResponse.ListAttachmentsMetadata(
        ctx,
        ticketId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// [beta: currently supported by Jira] Downloads the Attachment object matching {attachmentId} for the Ticket matching {tickedId} from the token-linked Integration.
func (c *Client) DownloadAttachment(
    ctx context.Context,
    ticketId engine.TicketId,
    attachmentId engine.AttachmentId,
    request *engine.DownloadAttachmentRequest,
    opts ...option.RequestOption,
) (*engine.DownloadAttachmentResponse, error){
    response, err := c.WithRawResponse.DownloadAttachment(
        ctx,
        ticketId,
        attachmentId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// [beta: currently supported by Jira] Deletes the Attachment object matching {attachmentId} for the Ticket matching {tickedId} from the token-linked Integration.
func (c *Client) DeleteAttachment(
    ctx context.Context,
    ticketId engine.TicketId,
    attachmentId engine.AttachmentId,
    request *engine.DeleteAttachmentRequest,
    opts ...option.RequestOption,
) error{
    _, err := c.WithRawResponse.DeleteAttachment(
        ctx,
        ticketId,
        attachmentId,
        request,
        opts...,
    )
    if err != nil {
        return err
    }
    return nil
}

// Lists all comments for the ticket matching {ticketId} from the token-linked Integration.
func (c *Client) ListComments(
    ctx context.Context,
    ticketId engine.TicketId,
    request *engine.ListCommentsRequest,
    opts ...option.RequestOption,
) (*engine.ListCommentsResponse, error){
    response, err := c.WithRawResponse.ListComments(
        ctx,
        ticketId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Creates a comment on the ticket matching {ticketId} from the token-linked Integration.
func (c *Client) CreateComment(
    ctx context.Context,
    ticketId engine.TicketId,
    request *engine.CreateCommentRequestInput,
    opts ...option.RequestOption,
) (*engine.CreateCommentResponse, error){
    response, err := c.WithRawResponse.CreateComment(
        ctx,
        ticketId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Deletes the comment matching {commentId} form the ticket matching {ticketId} from the token-linked Integration.
func (c *Client) DeleteComment(
    ctx context.Context,
    ticketId engine.TicketId,
    commentId engine.CommentId,
    request *engine.DeleteCommentRequest,
    opts ...option.RequestOption,
) error{
    _, err := c.WithRawResponse.DeleteComment(
        ctx,
        ticketId,
        commentId,
        request,
        opts...,
    )
    if err != nil {
        return err
    }
    return nil
}

// Creates a note on the ticket matching {ticketId} from the token-linked Integration.
func (c *Client) CreateNote(
    ctx context.Context,
    ticketId engine.TicketId,
    request *engine.CreateNoteRequestInput,
    opts ...option.RequestOption,
) (*engine.CreateNoteResponse, error){
    response, err := c.WithRawResponse.CreateNote(
        ctx,
        ticketId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Deletes the note matching {noteId} form the ticket matching {ticketId} from the token-linked Integration.
func (c *Client) DeleteNote(
    ctx context.Context,
    ticketId engine.TicketId,
    noteId engine.NoteId,
    request *engine.DeleteNoteRequest,
    opts ...option.RequestOption,
) error{
    _, err := c.WithRawResponse.DeleteNote(
        ctx,
        ticketId,
        noteId,
        request,
        opts...,
    )
    if err != nil {
        return err
    }
    return nil
}

// Lists all notes for the ticket matching {ticketId} from the token-linked Integration.
func (c *Client) ListNotes(
    ctx context.Context,
    ticketId engine.TicketId,
    request *engine.ListNotesRequest,
    opts ...option.RequestOption,
) (*engine.ListNotesResponse, error){
    response, err := c.WithRawResponse.ListNotes(
        ctx,
        ticketId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Update a note matching {noteId} title and/or content on the ticket matching {ticketId} from the token-linked Integration.
func (c *Client) PatchNote(
    ctx context.Context,
    ticketId engine.TicketId,
    noteId engine.NoteId,
    request *engine.PatchNoteRequestInput,
    opts ...option.RequestOption,
) (*engine.PatchNoteResponse, error){
    response, err := c.WithRawResponse.PatchNote(
        ctx,
        ticketId,
        noteId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Returns a list of escalation policies.
func (c *Client) QueryEscalationPolicies(
    ctx context.Context,
    request *engine.QueryEscalationPoliciesRequest,
    opts ...option.RequestOption,
) (*engine.QueryEscalationPoliciesResponse, error){
    response, err := c.WithRawResponse.QueryEscalationPolicies(
        ctx,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Returns a list of all on-call agents for an escalation policy.
func (c *Client) ListOnCall(
    ctx context.Context,
    escalationPolicyId engine.Id,
    request *engine.ListOnCallRequest,
    opts ...option.RequestOption,
) (*engine.ListOnCallResponse, error){
    response, err := c.WithRawResponse.ListOnCall(
        ctx,
        escalationPolicyId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

