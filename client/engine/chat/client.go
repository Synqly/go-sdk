// Code generated by Fern. DO NOT EDIT.

package chat

import (
    context "context"

    engine "github.com/synqly/go-sdk/client/engine"
    core "github.com/synqly/go-sdk/client/engine/core"
    internal "github.com/synqly/go-sdk/client/engine/internal"
    option "github.com/synqly/go-sdk/client/engine/option"
)


type Client struct {
    WithRawResponse *RawClient

    options *core.RequestOptions
    baseURL string
    caller *internal.Caller
}

func NewClient(options *core.RequestOptions) *Client {
    return &Client{
        WithRawResponse: NewRawClient(options),
        options: options,
        baseURL: options.BaseURL,
        caller: internal.NewCaller(
            &internal.CallerParams{
                Client: options.HTTPClient,
                MaxAttempts: options.MaxAttempts,
                DisableRetries: options.DisableRetries,
            },
        ),
    }
}

// Returns a list of users in the connected workspace or tenant. Used to discover whose conversations can be queried.
func (c *Client) QueryUsers(
    ctx context.Context,
    request *engine.QueryChatUsers,
    opts ...option.RequestOption,
) (*engine.ChatQueryUsersResponse, error){
    response, err := c.WithRawResponse.QueryUsers(
        ctx,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Returns a list of conversations scoped to a specific user — DMs, group chats, or (for AI providers) synthesised sessions.
func (c *Client) QueryUserConversations(
    ctx context.Context,
    userId string,
    request *engine.QueryUserConversations,
    opts ...option.RequestOption,
) (*engine.ChatQueryConversationsResponse, error){
    response, err := c.WithRawResponse.QueryUserConversations(
        ctx,
        userId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Returns the members of a conversation scoped to a specific user. Required for providers where conversations are only addressable via the user (e.g. Teams DMs, Copilot AI sessions).
func (c *Client) QueryUserConversationMembers(
    ctx context.Context,
    userId string,
    conversationId string,
    request *engine.QueryUserConversationMembers,
    opts ...option.RequestOption,
) (*engine.ChatQueryConversationMembersResponse, error){
    response, err := c.WithRawResponse.QueryUserConversationMembers(
        ctx,
        userId,
        conversationId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Returns messages from a conversation scoped to a specific user. Required for providers where conversations are only addressable via the user (e.g. Teams DMs, Copilot AI sessions).
func (c *Client) QueryUserConversationMessages(
    ctx context.Context,
    userId string,
    conversationId string,
    request *engine.QueryUserConversationMessages,
    opts ...option.RequestOption,
) (*engine.ChatQueryMessagesResponse, error){
    response, err := c.WithRawResponse.QueryUserConversationMessages(
        ctx,
        userId,
        conversationId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Returns a list of conversations (channels or workspaces) not scoped to a specific user. Providers that can only enumerate conversations per-user return NotImplemented; use query_user_conversations instead.
func (c *Client) QueryConversations(
    ctx context.Context,
    request *engine.QueryConversations,
    opts ...option.RequestOption,
) (*engine.ChatQueryConversationsResponse, error){
    response, err := c.WithRawResponse.QueryConversations(
        ctx,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Returns the members of a specific conversation. Use the user-scoped variant (query_user_conversation_members) for chats that only exist in a user's mailbox (e.g. Teams 1:1 chats).
func (c *Client) QueryConversationMembers(
    ctx context.Context,
    conversationId string,
    request *engine.QueryConversationMembers,
    opts ...option.RequestOption,
) (*engine.ChatQueryConversationMembersResponse, error){
    response, err := c.WithRawResponse.QueryConversationMembers(
        ctx,
        conversationId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

// Returns messages from a specific conversation. The conversationId must be obtained from query_conversations or query_user_conversations. Use the user-scoped variant (query_user_conversation_messages) for chats that only exist in a user's mailbox.
func (c *Client) QueryConversationMessages(
    ctx context.Context,
    conversationId string,
    request *engine.QueryConversationMessages,
    opts ...option.RequestOption,
) (*engine.ChatQueryMessagesResponse, error){
    response, err := c.WithRawResponse.QueryConversationMessages(
        ctx,
        conversationId,
        request,
        opts...,
    )
    if err != nil {
        return nil, err
    }
    return response.Body, nil
}

